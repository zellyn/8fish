# Standalone entropy-collector test image: one CODE segment loaded (and run)
# at $4000, like the engine's org, so the harness can load it raw.
MEMORY {
    MAIN: start = $4000, size = $7F00, file = %O;
}
SEGMENTS {
    CODE: load = MAIN, type = rw;
}
